interface:
  display_name: "EngineeringTeam"
  short_description: "One repo-first workflow for implementation, investigation, and handoff."
  default_prompt: "Use engineering-team to select the right implementation, investigation, or handoff route; map the relevant contracts, gather evidence, take only the authorized action, and verify or report the result."
policy:
  allow_implicit_invocation: true
