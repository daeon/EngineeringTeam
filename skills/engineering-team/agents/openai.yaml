interface:
  display_name: "EngineeringTeam"
  short_description: "Repo-first Codex workflow for safe software engineering changes."
  default_prompt: "Use engineering-team to map the repo, trace contracts, gather evidence, implement the smallest safe change, and verify it."
policy:
  allow_implicit_invocation: true
