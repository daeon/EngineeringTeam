{
  "name": "engineering-team",
  "version": "0.2.0",
  "description": "Expert-routed software engineering workflow for coding agents: a lead engineer selects the right panel, builds repo understanding, gates decisions with evidence, verifies changes, and preserves reusable knowledge.",
  "type": "module",
  "main": ".opencode/plugins/engineering-team.js",
  "scripts": {
    "validate": "python3 skills/engineering-team/scripts/validate-package.py && python3 scripts/validate-codex-package.py && python3 scripts/generate-agents.py --check && python3 scripts/sync-docs.py --check && python3 scripts/bump-version.py --check && node --check .opencode/plugins/engineering-team.js && npm run test:scripts && npm run test:examples",
    "test:scripts": "python3 -m unittest discover -s tests -v",
    "test:examples": "cd examples/buggy-python-service && python3 -m unittest discover -s tests -v",
    "sync:docs": "python3 scripts/sync-docs.py",
    "generate:agents": "python3 scripts/generate-agents.py",
    "check:agents": "python3 scripts/generate-agents.py --check",
    "doctor": "python3 scripts/doctor.py",
    "generate": "python3 scripts/generate-agents.py",
    "check:references": "python3 skills/engineering-team/scripts/validate-package.py"
  }
}
